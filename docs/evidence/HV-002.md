# HV-002 golden parent and first disposable child

## Result

Completed 2026-09-26 on host `LX101` after the owner's explicit authorization.
The Windows installation is preserved without Sysprep under the fixed-identity
boundary in [DEC-015](../DECISIONS.md#dec-015). No NVIDIA/GPU-P runtime, project
probe or AppSandbox component was added to the parent.

The owner explicitly chose not to block sealing on Windows activation or four
remaining offered Windows/Defender/.NET updates. The activation query returned
Windows 11 Pro Retail notification state (`LicenseStatus=5`); this is recorded as
a limitation, not a passing activation check.

## Inspected baseline

- VM `Win11`, GUID `2627e735-5b33-4104-b739-622727dd3a40`, was off and healthy,
  Generation 2/configuration `12.0`, Windows Secure Boot template enabled, vTPM
  enabled, unshielded, and had no GPU partition adapter.
- Guest: Windows 11 Pro x64 `10.0.26200` with installed `26200.9457` security
  update, computer `TESTVM`, workgroup, MachineGuid
  `046edc35-4c8f-4910-9c53-574681e623af`, `IMAGE_STATE_COMPLETE`.
- Guest had no CBS/Windows Update/file-rename reboot marker. Hyper-V integration
  version was `10.0.26200`; heartbeat, KVP, shutdown, time sync, PowerShell Direct
  and VSS services ran. Display inventory contained Microsoft Hyper-V Video and
  session Remote Display adapters only; no NVIDIA driver/device was present.
- C: was fully XTS-AES-128 encrypted but protection was suspended and no active
  protector was reported. The fixed VM shell/vTPM was retained; no encryption
  setting was changed.
- The original automatic checkpoint `eb88aa75-bbb0-4517-a1bf-b27be365e814`
  placed current state in `Win11_68600398-20F3-4E61-A426-B116C121955A.avhdx`.
  Automatic checkpoints were disabled before the inspection boot.

The owner-approved host reboot occurred at 2026-09-26 03:24:46 local time. Host
Secure Boot was enabled, CBS and Windows Update reboot markers were clear, and all
pinned VM/GPU identities were unchanged. `PendingFileRenameOperations` persisted
only as a delete request for Microsoft's
`C:\Windows\System32\gamingservicesproxy_13.dll.0`; both that file and its active
counterpart were identical version `10.0.26100.9441`. The plan was refreshed to
treat that exact reboot-persistent Gaming Services cleanup as unrelated noise; no
System32 file or registry value was deleted.

## Parent conversion

Before merging, Hyper-V exported the complete base/checkpoint/configuration chain to:

`Z:\HyperGpuSupport\recovery\pre-golden-merge-20260926T024758Z\`

Hyper-V then removed the one checkpoint and merged its AVHDX normally. The AVHDX
was not manually deleted. The original merged standalone disk remains at:

`C:\Users\dave\github\Hyper_GPU_Support\data\images\golden\Win11\Virtual Hard Disks\Win11.vhdx`

The merged disk was copied, byte-verified and retained at:

| Purpose | Path |
|---|---|
| Immutable parent | `Z:\HyperGpuSupport\images\golden\win11-pro-25h2-26200.9457-x64-v1\parent.vhdx` |
| Protected backup | `Z:\HyperGpuSupport\images\golden\win11-pro-25h2-26200.9457-x64-v1\backup\parent.vhdx` |

All three files measured 24,767,365,120 bytes and SHA-256
`0fb4dfe6dd51eed64d36e482e4f58d67c19922802e34aa6ae2d1f4ccd5daeb07`.
The golden directory has protected ACLs, and the parent has the NTFS read-only
attribute. SYSTEM/Administrators retain recovery control; the operator and exact VM
worker identity have read/execute access. The parent is not attached directly.

## Disposable slot and recovery proof

The existing VM configuration was retained as the only enrolled shell and moved to:

`Z:\HyperGpuSupport\images\disposable\gpu-pv-slot-01\vm\`

It is named `HyperGpuSupport-Disposable-01` and retains GUID, firmware, vTPM and
MAC identity. It now uses four vCPU, static 8 GiB memory, no automatic checkpoints,
automatic start `Nothing`, automatic stop `ShutDown`, no nested virtualization and
no mounted installation ISO. Its child is:

`Z:\HyperGpuSupport\images\disposable\gpu-pv-slot-01\child.vhdx`

`Get-VHD` verified a differencing VHDX whose parent is the exact immutable path.
The first child booted, returned the preserved `TESTVM`/MachineGuid and clean
display/reboot inventory through PowerShell Direct, and shut down gracefully.

A manual bounded reset first discarded the 1,067,450,368-byte booted child and
created a 4,194,304-byte fresh child without changing the parent hash. The installed
Rust runner then repeated reset from an unelevated trigger as operation
`1790391920-577596700`; its administrator-owned result reported success and the
same parent hash. That runner-created child booted successfully, preserved the same
guest identity and clean state, and shut down gracefully. Final VM state is off,
with zero checkpoints and zero GPU partition adapters.

## Remaining boundary

The installed runner currently permits only fixed `reset-slot`. CORE-005 remains
open for the other typed lifecycle/GPU/staging operations, minimum-rights work and
full client interface. Reset cannot select a VM, path or command and cannot mutate
the parent. Creating another VM shell or simultaneous descendant is not authorized
by this result and requires a generalized parent.
